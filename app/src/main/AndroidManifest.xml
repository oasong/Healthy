<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it59070098.kmitl.healthy">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".bmipage" />
        <activity android:name=".registerpage" />
        <activity android:name=".menupage" />
        <activity android:name=".weightpage" />
        <activity android:name=".addweightpage" />
        <activity android:name=".sleepform" />
        <activity android:name=".sleepitem" />
        <activity android:name=".sleep" />
        <activity android:name=".post" />
        <activity android:name=".commentpost"></activity>
    </application>

</manifest>